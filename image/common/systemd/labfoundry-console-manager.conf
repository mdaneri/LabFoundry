[Manager]
# tty1 belongs to the full-screen LabFoundry console. Keep unit progress in the
# journal so service start/stop messages cannot overwrite the curses display.
ShowStatus=no
# Reboot and shutdown are authenticated console actions. Do not let repeated
# Ctrl+Alt+Del keystrokes force a reboot around the masked target.
CtrlAltDelBurstAction=none
